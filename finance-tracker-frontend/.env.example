# Local:
# REACT_APP_API_BASE_URL=http://localhost:5001
#
# Production example:
REACT_APP_API_BASE_URL=https://<your-backend-project>.vercel.app

# Set true to serve the Budget Quest (v2) UI instead of the original Report view.
# Anything else, or unset, keeps the original UI. Remove this once v2 is cut over.
# REACT_APP_UI_V2=true
